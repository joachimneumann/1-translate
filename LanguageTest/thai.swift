// Note: This file is automatically generated
//       It will be overwritten when testing

import XCTest

final class Test_thai: XCTestCase {

    let language = Languages().thai

    func test_thai() {
        XCTAssertEqual(language.read(0).x, "ศูนย์")
        XCTAssertEqual(language.read(1).x, "หนึ่ง")
        XCTAssertEqual(language.read(2).x, "สอง")
        XCTAssertEqual(language.read(3).x, "สาม")
        XCTAssertEqual(language.read(4).x, "สี่")
        XCTAssertEqual(language.read(5).x, "ห้า")
        XCTAssertEqual(language.read(6).x, "หก")
        XCTAssertEqual(language.read(7).x, "เจ็ด")
        XCTAssertEqual(language.read(8).x, "แปด")
        XCTAssertEqual(language.read(9).x, "เก้า")
        XCTAssertEqual(language.read(10).x, "สิบ")
        XCTAssertEqual(language.read(11).x, "สิบเอ็ด")
        XCTAssertEqual(language.read(12).x, "สิบสอง")
        XCTAssertEqual(language.read(13).x, "สิบสาม")
        XCTAssertEqual(language.read(14).x, "สิบสี่")
        XCTAssertEqual(language.read(15).x, "สิบห้า")
        XCTAssertEqual(language.read(16).x, "สิบหก")
        XCTAssertEqual(language.read(17).x, "สิบเจ็ด")
        XCTAssertEqual(language.read(18).x, "สิบแปด")
        XCTAssertEqual(language.read(19).x, "สิบเก้า")
        XCTAssertEqual(language.read(20).x, "ยี่สิบ")
        XCTAssertEqual(language.read(21).x, "ยี่สิบเอ็ด")
        XCTAssertEqual(language.read(22).x, "ยี่สิบสอง")
        XCTAssertEqual(language.read(23).x, "ยี่สิบสาม")
        XCTAssertEqual(language.read(24).x, "ยี่สิบสี่")
        XCTAssertEqual(language.read(25).x, "ยี่สิบห้า")
        XCTAssertEqual(language.read(26).x, "ยี่สิบหก")
        XCTAssertEqual(language.read(27).x, "ยี่สิบเจ็ด")
        XCTAssertEqual(language.read(28).x, "ยี่สิบแปด")
        XCTAssertEqual(language.read(29).x, "ยี่สิบเก้า")
        XCTAssertEqual(language.read(30).x, "สามสิบ")
        XCTAssertEqual(language.read(31).x, "สามสิบเอ็ด")
        XCTAssertEqual(language.read(32).x, "สามสิบสอง")
        XCTAssertEqual(language.read(33).x, "สามสิบสาม")
        XCTAssertEqual(language.read(34).x, "สามสิบสี่")
        XCTAssertEqual(language.read(35).x, "สามสิบห้า")
        XCTAssertEqual(language.read(36).x, "สามสิบหก")
        XCTAssertEqual(language.read(37).x, "สามสิบเจ็ด")
        XCTAssertEqual(language.read(38).x, "สามสิบแปด")
        XCTAssertEqual(language.read(39).x, "สามสิบเก้า")
        XCTAssertEqual(language.read(40).x, "สี่สิบ")
        XCTAssertEqual(language.read(41).x, "สี่สิบเอ็ด")
        XCTAssertEqual(language.read(42).x, "สี่สิบสอง")
        XCTAssertEqual(language.read(43).x, "สี่สิบสาม")
        XCTAssertEqual(language.read(44).x, "สี่สิบสี่")
        XCTAssertEqual(language.read(45).x, "สี่สิบห้า")
        XCTAssertEqual(language.read(46).x, "สี่สิบหก")
        XCTAssertEqual(language.read(47).x, "สี่สิบเจ็ด")
        XCTAssertEqual(language.read(48).x, "สี่สิบแปด")
        XCTAssertEqual(language.read(49).x, "สี่สิบเก้า")
        XCTAssertEqual(language.read(50).x, "ห้าสิบ")
        XCTAssertEqual(language.read(51).x, "ห้าสิบเอ็ด")
        XCTAssertEqual(language.read(52).x, "ห้าสิบสอง")
        XCTAssertEqual(language.read(53).x, "ห้าสิบสาม")
        XCTAssertEqual(language.read(54).x, "ห้าสิบสี่")
        XCTAssertEqual(language.read(55).x, "ห้าสิบห้า")
        XCTAssertEqual(language.read(56).x, "ห้าสิบหก")
        XCTAssertEqual(language.read(57).x, "ห้าสิบเจ็ด")
        XCTAssertEqual(language.read(58).x, "ห้าสิบแปด")
        XCTAssertEqual(language.read(59).x, "ห้าสิบเก้า")
        XCTAssertEqual(language.read(60).x, "หกสิบ")
        XCTAssertEqual(language.read(61).x, "หกสิบเอ็ด")
        XCTAssertEqual(language.read(62).x, "หกสิบสอง")
        XCTAssertEqual(language.read(63).x, "หกสิบสาม")
        XCTAssertEqual(language.read(64).x, "หกสิบสี่")
        XCTAssertEqual(language.read(65).x, "หกสิบห้า")
        XCTAssertEqual(language.read(66).x, "หกสิบหก")
        XCTAssertEqual(language.read(67).x, "หกสิบเจ็ด")
        XCTAssertEqual(language.read(68).x, "หกสิบแปด")
        XCTAssertEqual(language.read(69).x, "หกสิบเก้า")
        XCTAssertEqual(language.read(70).x, "เจ็ดสิบ")
        XCTAssertEqual(language.read(71).x, "เจ็ดสิบเอ็ด")
        XCTAssertEqual(language.read(72).x, "เจ็ดสิบสอง")
        XCTAssertEqual(language.read(73).x, "เจ็ดสิบสาม")
        XCTAssertEqual(language.read(74).x, "เจ็ดสิบสี่")
        XCTAssertEqual(language.read(75).x, "เจ็ดสิบห้า")
        XCTAssertEqual(language.read(76).x, "เจ็ดสิบหก")
        XCTAssertEqual(language.read(77).x, "เจ็ดสิบเจ็ด")
        XCTAssertEqual(language.read(78).x, "เจ็ดสิบแปด")
        XCTAssertEqual(language.read(79).x, "เจ็ดสิบเก้า")
        XCTAssertEqual(language.read(80).x, "แปดสิบ")
        XCTAssertEqual(language.read(81).x, "แปดสิบเอ็ด")
        XCTAssertEqual(language.read(82).x, "แปดสิบสอง")
        XCTAssertEqual(language.read(83).x, "แปดสิบสาม")
        XCTAssertEqual(language.read(84).x, "แปดสิบสี่")
        XCTAssertEqual(language.read(85).x, "แปดสิบห้า")
        XCTAssertEqual(language.read(86).x, "แปดสิบหก")
        XCTAssertEqual(language.read(87).x, "แปดสิบเจ็ด")
        XCTAssertEqual(language.read(88).x, "แปดสิบแปด")
        XCTAssertEqual(language.read(89).x, "แปดสิบเก้า")
        XCTAssertEqual(language.read(90).x, "เก้าสิบ")
        XCTAssertEqual(language.read(91).x, "เก้าสิบเอ็ด")
        XCTAssertEqual(language.read(92).x, "เก้าสิบสอง")
        XCTAssertEqual(language.read(93).x, "เก้าสิบสาม")
        XCTAssertEqual(language.read(94).x, "เก้าสิบสี่")
        XCTAssertEqual(language.read(95).x, "เก้าสิบห้า")
        XCTAssertEqual(language.read(96).x, "เก้าสิบหก")
        XCTAssertEqual(language.read(97).x, "เก้าสิบเจ็ด")
        XCTAssertEqual(language.read(98).x, "เก้าสิบแปด")
        XCTAssertEqual(language.read(99).x, "เก้าสิบเก้า")
        XCTAssertEqual(language.read(100).x, "หนึ่งร้อย")
        XCTAssertEqual(language.read(101).x, "หนึ่งร้อยหนึ่ง")
        XCTAssertEqual(language.read(102).x, "หนึ่งร้อยสอง")
        XCTAssertEqual(language.read(103).x, "หนึ่งร้อยสาม")
        XCTAssertEqual(language.read(104).x, "หนึ่งร้อยสี่")
        XCTAssertEqual(language.read(105).x, "หนึ่งร้อยห้า")
        XCTAssertEqual(language.read(106).x, "หนึ่งร้อยหก")
        XCTAssertEqual(language.read(107).x, "หนึ่งร้อยเจ็ด")
        XCTAssertEqual(language.read(108).x, "หนึ่งร้อยแปด")
        XCTAssertEqual(language.read(109).x, "หนึ่งร้อยเก้า")
        XCTAssertEqual(language.read(110).x, "หนึ่งร้อยสิบ")
        XCTAssertEqual(language.read(111).x, "หนึ่งร้อยสิบเอ็ด")
        XCTAssertEqual(language.read(112).x, "หนึ่งร้อยสิบสอง")
        XCTAssertEqual(language.read(113).x, "หนึ่งร้อยสิบสาม")
        XCTAssertEqual(language.read(119).x, "หนึ่งร้อยสิบเก้า")
        XCTAssertEqual(language.read(120).x, "หนึ่งร้อยยี่สิบ")
        XCTAssertEqual(language.read(121).x, "หนึ่งร้อยยี่สิบเอ็ด")
        XCTAssertEqual(language.read(126).x, "หนึ่งร้อยยี่สิบหก")
        XCTAssertEqual(language.read(190).x, "หนึ่งร้อยเก้าสิบ")
        XCTAssertEqual(language.read(200).x, "สองร้อย")
        XCTAssertEqual(language.read(201).x, "สองร้อยหนึ่ง")
        XCTAssertEqual(language.read(400).x, "สี่ร้อย")
        XCTAssertEqual(language.read(444).x, "สี่ร้อยสี่สิบสี่")
        XCTAssertEqual(language.read(500).x, "ห้าร้อย")
        XCTAssertEqual(language.read(580).x, "ห้าร้อยแปดสิบ")
        XCTAssertEqual(language.read(999).x, "เก้าร้อยเก้าสิบเก้า")
        XCTAssertEqual(language.read(1_000).x, "หนึ่งพัน")
        XCTAssertEqual(language.read(1_001).x, "หนึ่งพันหนึ่ง")
        XCTAssertEqual(language.read(1_005).x, "หนึ่งพันห้า")
        XCTAssertEqual(language.read(1_009).x, "หนึ่งพันเก้า")
        XCTAssertEqual(language.read(1_010).x, "หนึ่งพันสิบ")
        XCTAssertEqual(language.read(1_011).x, "หนึ่งพันสิบเอ็ด")
        XCTAssertEqual(language.read(1_012).x, "หนึ่งพันสิบสอง")
        XCTAssertEqual(language.read(1_015).x, "หนึ่งพันสิบห้า")
        XCTAssertEqual(language.read(1_035).x, "หนึ่งพันสามสิบห้า")
        XCTAssertEqual(language.read(1_099).x, "หนึ่งพันเก้าสิบเก้า")
        XCTAssertEqual(language.read(1_100).x, "หนึ่งพันหนึ่งร้อย")
        XCTAssertEqual(language.read(1_101).x, "หนึ่งพันหนึ่งร้อยหนึ่ง")
        XCTAssertEqual(language.read(1_105).x, "หนึ่งพันหนึ่งร้อยห้า")
        XCTAssertEqual(language.read(1_110).x, "หนึ่งพันหนึ่งร้อยสิบ")
        XCTAssertEqual(language.read(1_111).x, "หนึ่งพันหนึ่งร้อยสิบเอ็ด")
        XCTAssertEqual(language.read(1_115).x, "หนึ่งพันหนึ่งร้อยสิบห้า")
        XCTAssertEqual(language.read(1_121).x, "หนึ่งพันหนึ่งร้อยยี่สิบเอ็ด")
        XCTAssertEqual(language.read(1_125).x, "หนึ่งพันหนึ่งร้อยยี่สิบห้า")
        XCTAssertEqual(language.read(3_005).x, "สามพันห้า")
        XCTAssertEqual(language.read(3_105).x, "สามพันหนึ่งร้อยห้า")
        XCTAssertEqual(language.read(3_155).x, "สามพันหนึ่งร้อยห้าสิบห้า")
        XCTAssertEqual(language.read(8_000).x, "แปดพัน")
        XCTAssertEqual(language.read(8_126).x, "แปดพันหนึ่งร้อยยี่สิบหก")
        XCTAssertEqual(language.read(10_000).x, "หนึ่งหมื่น")
        XCTAssertEqual(language.read(10_001).x, "หนึ่งหมื่นหนึ่ง")
        XCTAssertEqual(language.read(20_000).x, "สองหมื่น")
        XCTAssertEqual(language.read(20_001).x, "สองหมื่นหนึ่ง")
        XCTAssertEqual(language.read(20_010).x, "สองหมื่นสิบ")
        XCTAssertEqual(language.read(20_101).x, "สองหมื่นหนึ่งร้อยหนึ่ง")
        XCTAssertEqual(language.read(100_000).x, "หนึ่งแสน")
        XCTAssertEqual(language.read(100_001).x, "หนึ่งแสนหนึ่ง")
        XCTAssertEqual(language.read(200_000).x, "สองแสน")
        XCTAssertEqual(language.read(201_000).x, "สองแสนหนึ่งพัน")
        XCTAssertEqual(language.read(200_001).x, "สองแสนหนึ่ง")
        XCTAssertEqual(language.read(201_001).x, "สองแสนหนึ่งพันหนึ่ง")
        XCTAssertEqual(language.read(300_000).x, "สามแสน")
        XCTAssertEqual(language.read(300_451).x, "สามแสนสี่ร้อยห้าสิบเอ็ด")
        XCTAssertEqual(language.read(502_305).x, "ห้าแสนสองพันสามร้อยห้า")
        XCTAssertEqual(language.read(999_999).x, "เก้าแสนเก้าหมื่นเก้าพันเก้าร้อยเก้าสิบเก้า")
        XCTAssertEqual(language.read(1_000_000).x, "หนึ่งล้าน")
        XCTAssertEqual(language.read(21_700_098).x, "ยี่สิบเอ็ดล้านเจ็ดแสนเก้าสิบแปด")
        XCTAssertEqual(language.read(62_000_000).x, "หกสิบสองล้าน")
        XCTAssertEqual(language.read(62_003_005).x, "หกสิบสองล้านสามพันห้า")
        XCTAssertEqual(language.read(62_003_105).x, "หกสิบสองล้านสามพันหนึ่งร้อยห้า")
        XCTAssertEqual(language.read(62_003_155).x, "หกสิบสองล้านสามพันหนึ่งร้อยห้าสิบห้า")
        XCTAssertEqual(language.read(999_999_999).x, "เก้าร้อยเก้าสิบเก้าล้านเก้าแสนเก้าหมื่นเก้าพันเก้าร้อยเก้าสิบเก้า")
        XCTAssertEqual(language.read(99_000_000_909).x, "เก้าสิบเก้าพันล้านเก้าร้อยเก้า")
        XCTAssertEqual(language.read(99_999_999_999).x, "เก้าสิบเก้าพันล้านเก้าร้อยเก้าสิบเก้าล้านเก้าแสนเก้าหมื่นเก้าพันเก้าร้อยเก้าสิบเก้า")
        XCTAssertEqual(language.read(1_000_000_000_000).x, "หนึ่งล้านล้าน")
        XCTAssertEqual(language.read(100_000_000_000_000).x, "หนึ่งร้อยล้านล้าน")
        XCTAssertEqual(language.read(-1).x, "ลบหนึ่ง")

        let temp1 = "-1.5"
        XCTAssertEqual(language.read(temp1).x, "ลบหนึ่งจุดห้า")
        // "1.5" หนึ่งจุดห้า
        // "3.1415926" สามจุดหนึ่งสี่หนึ่งห้าเก้าสองหก

        let temp2 = "-3.1415926"
        XCTAssertEqual(language.read(temp2).x, "ลบสามจุดหนึ่งสี่หนึ่งห้าเก้าสองหก")
        // "3.4e13" สามจุดสี่เท่าสิบถึงพลังของสิบสาม
        // 8,126 — eight is in the thousand digit spot, so we read “eight” followed by the name of the thousand digit in Thai which is แปดพัน (bpàaet phan).
        // 8,126 — one is in the hundred digit spot, so we read “one” in Thai followed by the name of the hundred digit in Thai which is หนึ่งร้อย (nùeng ráauy).
        // 8,126 — two is in the ten digit spot, so we read “two” followed by the name of the ten digit in Thai which is ยี่สิบ (yîi sìp). (Don’t forget that for 20, Thai people read it ยี่สิบ [yîi sìp]).
        // 8,126 — six is in the unit digit spot, so we read “six” in Thai which is หก (hòk). (Don’t forget that for the unit digit, you read only the number and don’t read the name of the digit.)
        // 8,126 is แปดพันหนึ่งร้อยยี่สิบหก (bpàaet phan nùeng ráauy yîi sìp hòk).
        // 300,451 — three is in the hundred-thousand digit spot so we read “three” in Thai followed by the name of the hundred-thousand digit which is สามแสน (sǎam sǎaen).
        // 300,451 — since the number in the ten-thousand and thousand digit spots is 0, we can skip reading these two digits.
        // 300,451 — four is in the hundred digit spot, so we read “four” followed by the name of the hundred digit in Thai which is สี่ร้อย (sìi ráauy).
        // 300,451 — five is in the ten digit spot, so we read “five” followed by the name of the ten digit in Thai which is ห้าสิบ (hâa sìp).
        // 300,451 — one is in the unit digit spot, so we read one in Thai which is เอ็ด (èt). (Don’t forget that for numbers higher than 10, if 1 is in the unit digit spot, we read it เอ็ด [èt].)
        // 300,451 is สามแสนสี่ร้อยห้าสิบเอ็ด (sǎam sǎaen sìi ráauy hâa sìp èt).
        // Example of 21,700,098
        // 21 and 700,098 — you divide the number into parts for every six digits from right to left first, so you get 21 and 700,098.
        // 21,700,098 — you read the left part first followed by ล้าน (láan) which is ยี่สิบเอ็ดล้าน (yîi sìp èt láan).
        // 21,700,098 — you read the right part after that which is เจ็ดแสนเก้าสิบแปด (jèt sǎaen gâo sìp bpàaet).
        // 21,700,098 is ยี่สิบเอ็ดล้านเจ็ดแสนเก้าสิบแปด (yîi sìp èt láan jèt sǎaen gâo sìp bpàaet).
    }
}
